# M1 — Jigsaw DistilBERT training.
#
# REQUIRES PYTHON 3.12 (see .python-version). torch 2.5.1 ships no wheels
# above cp312, and download.pytorch.org prunes old releases from its index,
# so a 3.13+ venv fails to resolve with a misleading "no matching
# distribution" naming only torch. numpy 1.26.4 and pandas 2.1.4 have the
# same ceiling. Create the venv explicitly:
#     ~/.pyenv/versions/3.12.13/bin/python -m venv .venv
#
# CPU-only build of PyTorch. Smaller install than the CUDA wheel and
# plenty for short DistilBERT runs on the laptop.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.5.1
transformers==4.46.3
accelerate==1.2.1
datasets==3.1.0
scikit-learn==1.5.2
pandas==2.1.4
numpy==1.26.4

# ONNX export (M2 GPU engine build)
onnx==1.22.0

# Tracking + dataset download
mlflow==2.20.3
kagglehub==0.4.1

# Pinning note (README.md "Known limitations"):
# chart-side versions are placeholders; this client-side pin matches the
# in-cluster MLflow image (ghcr.io/mlflow/mlflow:v2.20.3) so client/server
# REST API versions agree.
