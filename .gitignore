# Machine-local notes, never committed
CAVEATS.local.md
*.local.md

# Secrets — never commit
kaggle.json
access_token
.env
.env.*
!.env.example
*.pem
*.key

# Dev notes — ADRs are kept local, not published with the repo
docs/

# IDE / editor state
.idea/
.vscode/
*.swp

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/

# Training artifacts
training/runs/
training/data/
mlruns/
*.ckpt
*.pt
*.bin
*.safetensors

# Model binaries (built from source, not versioned)
*.onnx
*.plan
model-repo/
